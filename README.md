# 🎨 Pixel Mood Board
**Варіант:** Варіант 2 (Малювання)  
**Мова реалізації:** JavaScript + HTML + CSS  
**Запуск:** через Live Server (WebStorm / VS Code)

---

## 💡 Ідея програми
**Pixel Mood Board** — це проста графічна програма, у якій користувач може створювати зображення з пікселів, вибирати кольори, експериментувати з композицією та навіть «намалювати» заголовок власноруч.  
Це реалізація варіанту № 2 (малювання).

Програма відкривається локально як окрема сторінка і працює повністю без інтернету.

---

## ⚙️ Функціональні можливості

### 🔸 Базові
1. Вибір кольору кисті (колірна палета).
2. Полотно 8 × 8 з пікселів.
3. Малювання кліком по клітинці.
4. Кнопки **Очистити**, **Random color**, **Undo/Redo**.
5. Вибір кольору фону сторінки.
6. Збереження та відновлення малюнка (через `localStorage`).
7. Лічильник заповнених пікселів.

### 🔹 Додаткові
1. «Піпетка» — вибір кольору з існуючого пікселя.
2. Зона «Pixel Title» — можливість намалювати заголовок пікселями.
3. Історія дій (таблиця подій у нижній панелі).
4. Експорт зображення у формат PNG.
5. Перемикання між світлою та темною темами (фон сторінки).

---

## ✅ Відповідність вимогам

- **Інтерфейсні зони (4+)**:
    1. Панель інструментів (toolbar);
    2. Полотно (pixel grid);
    3. Панель підказок гарячих клавіш;
    4. Таблиця «Історія дій».

- **Контроли (20+)**:
    - 64 клітинки полотна;
    - кнопки `Clear`, `Undo`, `Redo`, `Save`, `Load`, `Export`, `Random color`, `Піпетка`, `Apply background`;
    - 2 поля вибору кольору;
    - разом понад 70 елементів керування.

- **Обробники подій (10+)**:
    - натискання на клітинки;
    - усі кнопки toolbar;
    - гарячі клавіші (Z/Y/C/R/S/L/E/P/B);
    - події збереження/завантаження, експорту тощо.

- **Контейнер даних (1+)**:
    - Таблиця `Історія дій`, у якій відображаються усі виконані команди користувача.

- **Додатково реалізовано**:
    - Збереження стану у `localStorage`;
    - Експорт малюнка у форматі PNG;
    - Випадкове генерування кольору;
    - Гарячі клавіші для основних дій;
    - Піпетка для вибору кольору з полотна.

---

## 🚀 Запуск

1. Відкрити проєкт.
2. Запустити за допомогою **Live Server**.
3. При першому запуску програма створює пусту сітку 8×8.
4. Усі дані можна зберегти (`Save`) і відновити (`Load`).
5. Для експорту натиснути `Export PNG`.

---

## 🧱 Структура проєкту
```
pixel-mood-board/
 ├── index.html        # головна сторінка
 ├── style.css         # стилі оформлення
 ├── script.js         # логіка роботи програми
 ├── tests.js          # тестування окремих функцій
 ├── README.md         # опис проєкту
 └── .gitignore        # ігноровані службові файли
```

---

## 🧪 Тестування
- Файл `tests.js` виконує автоматичну перевірку основних функцій.
