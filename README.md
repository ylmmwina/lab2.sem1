# 🎨 Pixel Mood Board

**Варіант:** 2 — Малювання  
**Мова реалізації:** JavaScript + HTML + CSS  
**Запуск:** через Live Server (WebStorm / VS Code) — сторінка `index.html`

---

## 💡 Ідея програми

**Pixel Mood Board** — це невелика графічна програма, у якій користувач може:

- малювати піксельні зображення на сітці 8×8;
- обирати колір «кисті» та фону сторінки;
- зберігати/завантажувати свої роботи;
- експортувати малюнок у PNG;
- зберігати кілька різних варіантів як пресети у галереї;
- керувати виглядом інтерфейсу через налаштування.

Програма працює **локально**, без інтернету, як окремий застосунок у браузері.

---

## ⚙️ Функціональні можливості

### 🔸 Базові

1. **Палітра кольору кисті**
    - Колірний інпут для вибору кольору, яким малюються пікселі.

2. **Полотно 8×8**
    - Сітка з 64 клітинок (кнопок), кожна з яких може мати власний колір.

3. **Малювання кліком**
    - Клік по клітинці → клітинка фарбується обраним кольором.

4. **Основні операції над полотном**
    - `Clear` — очищення всіх клітинок;
    - `Undo` — відміна останньої дії (на основі історії знімків стану);
    - `Redo` — повтор відміненої дії.

5. **Колір фону сторінки**
    - Окремий інпут для вибору кольору фону застосунку;
    - Кнопка `Apply background` застосовує вибраний фон.

6. **Лічильник заповнених клітинок**
    - Показує кількість пікселів, які мають встановлений колір.

### 🔹 Робота зі збереженням

7. **Збереження стану полотна (`Save`)**
    - Зберігає поточний масив кольорів у `localStorage`.

8. **Завантаження стану (`Load`)**
    - Відновлює полотно із збереженого стану.

9. **Експорт у PNG (`Export`)**
    - Генерація зображення 8×8 у вигляді PNG-файлу (`pixel-mood.png`) із поточних кольорів.

### 🔹 Додаткові інструменти

10. **Random color**
    - Випадковий колір у форматі `#RRGGBB` підставляється в палітру кисті.

11. **Піпетка (`Піпетка`)**
    - Режим «піпетки»: натискаємо кнопку → клікаємо по клітинці → колір клітинки копіюється у палітру;
    - Повторне натискання або вибір кольору вимикає режим піпетки.

12. **Гарячі клавіші (створення «гарячого» інтерфейсу)**
    - `Z` — Undo
    - `Y` — Redo
    - `C` — Clear
    - `R` — Random color
    - `S` — Save
    - `L` — Load
    - `E` — Export PNG
    - `P` — Піпетка (on/off)
    - `B` — Apply background

Опис гарячих клавіш продубльовано у вкладці **Довідка**.

### 🔹 Галерея пресетів

13. **Збереження пресету (`💾 Зберегти`)**
    - Зберігає поточний малюнок як окремий пресет із назвою (через `prompt`).

14. **Завантаження пресету (`📥 Завантажити`)**
    - Вибір пресету зі списку та відновлення стану полотна з нього.

15. **Видалення пресету (`🗑️ Видалити`)**
    - Видаляє обраний пресет зі сховища й оновлює список.

### 🔹 Налаштування

16. **Показувати межі клітинок**
    - Чекбокс `Показувати межі клітинок` додає/знімає клас `.with-border` у всіх клітинок;
    - Стан зберігається у `localStorage`, застосовується при наступному запуску.

17. **Звук кліку (демо)**
    - Чекбокс `Звук кліку (демо)` вмикає логування події малювання (можна розширити до реального звуку);
    - Стан також зберігається у `localStorage`.

18. **Скинути застосунок (`🔄 Скинути застосунок`)**
    - Очищає полотно, історію дій, збережений стан, налаштування меж/звуку й скидає чекбокси до початкових значень.

---

## 🧩 Інтерфейсні зони (вікна / в'юхи)

Інтерфейс організовано через вкладки (імітовані «вікна»), кожна з яких відповідає вимогам до окремих областей:

1. **Полотно (вкладка «Полотно»)**
    - Панель інструментів (toolbar) з кнопками й полями введення;
    - Сітка 8×8 для малювання;
    - Лічильник заповнених клітинок.

2. **Галерея (вкладка «Галерея»)**
    - Кнопки `Зберегти`, `Завантажити`, `Видалити`;
    - Список (select) пресетів;
    - Текстовий опис.

3. **Налаштування (вкладка «Налаштування»)**
    - Чекбокси для меж і звуку;
    - Кнопка «Скинути застосунок».

4. **Довідка (вкладка «Довідка»)**
    - Блок із описом гарячих клавіш у вигляді списку;
    - Додаткова інформація про управління програмою.

5. **Історія дій (внизу основної вкладки)**
    - Таблиця з часом/індексом дії, типом та деталями;
    - Динамічно доповнюється під час роботи.

Таким чином, у застосунку є **4+ окремих “вікна” інтерфейсу** з різним вмістом і логікою.

---

## 🕹️ Елементи керування (контроли)

### Toolbar (основна панель)

1. Інпут кольору кисті (color input).
2. Інпут кольору фону сторінки.
3. Кнопка `Clear`.
4. Кнопка `Undo`.
5. Кнопка `Redo`.
6. Кнопка `Save`.
7. Кнопка `Load`.
8. Кнопка `Export`.
9. Кнопка `Random color`.
10. Кнопка `Піпетка`.
11. Кнопка `Apply background`.

### Полотно

12. **64 кнопки-пікселі** (кожна клітинка — окремий елемент керування).

### Галерея

13. Кнопка `💾 Зберегти` (зберегти пресет).
14. Кнопка `📥 Завантажити` (завантажити пресет).
15. Кнопка `🗑️ Видалити` (видалити пресет).
16. Список пресетів (`select` з опціями).

### Налаштування

17. Чекбокс `Показувати межі клітинок`.
18. Чекбокс `Звук кліку (демо)`.
19. Кнопка `🔄 Скинути застосунок`.

### Навігація по вкладках

20. Кнопка-вкладка `Полотно`.
21. Кнопка-вкладка `Галерея`.
22. Кнопка-вкладка `Налаштування`.
23. Кнопка-вкладка `Довідка`.

> Навіть якщо **не рахувати** 64 клітинки як «контроли», залишаються 20+ повноцінних керуючих елементів. З урахуванням сітки — значно більше.

---

## 🔁 Обробники подій

У застосунку використовується **10+ обробників подій**, зокрема:

- Клік по кожній клітинці сітки (малювання / піпетка).
- Клік по кнопках toolbar: `Clear`, `Undo`, `Redo`, `Save`, `Load`, `Export`, `Random color`, `Піпетка`, `Apply background`.
- Клік по кнопках галереї: `Зберегти`, `Завантажити`, `Видалити`.
- Вибір пресету зі списку (подія `change`).
- Чекбокси в налаштуваннях: `toggle-borders`, `toggle-sound` (події `change`).
- Кнопка `Скинути застосунок`.
- Перемикання вкладок (події кліку в `tabs.js`).
- Обробник `keydown` для гарячих клавіш (`Z/Y/C/R/S/L/E/P/B`).

---

## 📦 Контейнери даних

- **Масив стану полотна**
    - `gridState` — масив довжиною 64, кожен елемент — рядок з кольором або `''`.

- **Історія undo/redo**
    - `history`, `redoStack` — стеки знімків стану для можливості повернення/повтору дій.

- **Галерея пресетів**
    - Масив об’єктів `{ id, name, data }`, який зберігається в `localStorage` під ключем `pixel-mood:presets`.

- **Налаштування**
    - Значення прапорців зберігаються в `localStorage` (`pixel-mood:settings:borders`, `pixel-mood:settings:sound`).

- **Таблиця історії дій**
    - DOM-таблиця, що динамічно поповнюється записами про дії (тип і деталі дій).

---

## 🧱 Структура проєкту

```text
pixel-mood-board/
 ├── index.html          # головна сторінка, розмітка вкладок і блоків
 ├── style.css           # стилі оформлення (полотно, заголовок, кнопки, історія)
 ├── tabs.css            # стилі для вкладок і карток у вкладках
 ├── images/             
 │   ├── icon.png        # піксельна іконка, намальована вручну
 │   ├── name.png        # піксельний заголовок, намальований вручну
 ├── js/
 │   ├── app.js          # точка входу, з’єднує всі модулі й DOM
 │   ├── state.js        # стан полотна, рендер та лічильник
 │   ├── history.js      # логіка undo/redo
 │   ├── grid.js         # створення сітки, paint, clear
 │   ├── palette.js      # randomColor, pipette
 │   ├── background.js   # керування фоном сторінки
 │   ├── storage.js      # save/load у localStorage
 │   ├── exporter.js     # експорт у PNG
 │   ├── settings.js     # межі клітинок, звук, reset застосунку
 │   └── gallery.js      # збереження/завантаження/видалення пресетів
 ├── tabs.js             # логіка перемикання вкладок
 ├── tests/
 │   ├── core.js         # простий тест-раннер (assert, test, report)
 │   ├── state.test.js   # тести для модуля state.js
 │   ├── grid.test.js    # тести для grid.js
 │   ├── history.test.js # тести для history.js (undo/redo)
 │   ├── palette.test.js # тести для palette.js
 │   ├── gallery.test.js # тести для gallery.js (пресети)
 │   ├── settings.test.js# тести для settings.js
 │   └── index.js        # підключає всі тести та виводить результат у консоль
 ├── README.md           # опис проєкту (цей файл)
 └── .gitignore          # ігнор проєктних файлів середовища (WebStorm тощо)
 ```
 
## 🧪 Модульне тестування

У проєкті реалізовано повний пакет модульних тестів, організований за принципом «1 модуль — 1 тестовий файл».  
Тести виконуються прямо у браузері через консоль і покривають основну функціональність застосунку.

---

### 📁 Структура тестів

```
tests/
├── core.js # власний тест-раннер
├── index.js # збирає й запускає всі тести
├── state.test.js # тестування модуля state.js
├── grid.test.js # тестування роботи полотна
├── history.test.js # undo / redo
├── palette.test.js # randomColor, pipette
├── gallery.test.js # пресети (save/load/remove)
└── settings.test.js # межі клітинок та reset
```


---

### ⚙️ Test runner (`core.js`)

Створений власний мінімалістичний тест-раннер, що містить:

- `test(name, fn)` — опис тесту
- `assert`, `assertEqual`, `assertArrayEquals` — асерти
- `report()` — вивід загального результату
- масив результатів `results[]`

Це дозволяє запускати тести без залежностей на сторонні бібліотеки.

---

### 🧪 Покриття модулів

#### ✔ **state.test.js**
- ініціалізація стану 8×8;
- snapshot() повертає глибоку копію;
- setGridState() правильно оновлює стан.

#### ✔ **grid.test.js**
- створення сітки з 64 клітинок;
- фарбування (`paint`) змінює стан та лічильник;
- очищення (`clearGrid`) скидає полотно.

#### ✔ **history.test.js**
- undo повертає попередній знімок стану;
- redo повертає наступний;
- коректна поведінка стеків.

#### ✔ **palette.test.js**
- randomColor генерує коректний CSS-формат `#RRGGBB`;
- pipette бере колір з клітинки та вимикає режим.

#### ✔ **gallery.test.js**
- збереження пресету (через prompt);
- завантаження пресету;
- видалення пресету;
- звіряння з localStorage.

#### ✔ **settings.test.js**
- перемикач меж (`toggle-borders`) працює і зберігається у localStorage;
- toggle-sound перемикає режим звуку;
- reset очищає:
    - gridState
    - localStorage
    - історію дій
    - UI-елементи (чекбокси)

---

### ▶️ Запуск тестів

Тести підключено у кінці `index.html`:

```html
<script type="module" src="tests/index.js"></script>
```

Для запуску:
- Відкрити index.html через Live Server
- Відкрити консоль (F12 → Console)
- Отримати результат